// interface Option {
//   id: number;
//   name: string;
// }

// interface InputFieldProps {
//   label: string;
//   options: Option[];
// };

function InputField(/* { label, options }: InputFieldProps */) {
  return (
    <div>
      {/* <label>{label}</label>
      <select>
        {options.map(item => <option key={item.id}>{item.name}</option>)}
      </select> */}
    </div >
  );
}

export default InputField;
